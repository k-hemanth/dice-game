export type THistoryBalance = {
  date: string;
  balance: number;
};

export interface TimelineEntryProps {
  balance: number;
}

export interface ButtonData {
  diceValue: number;
  count: number;
}
